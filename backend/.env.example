# Render Dashboard → Environment → qo‘shing (yoki mahalliy: backend/.env nomiga nusxa oling)
#
# Supabase: Project Settings → Database → URI → Transaction pooler (port 6543).
# Parolda @ : # % bo‘lsa, URL ichida encode qiling (Python: urllib.parse.quote_plus("parol")).
# Hech qachon hostname oldidan nuqta yoki bo‘sh qator qo‘ymang.

DATABASE_URL=postgresql://postgres.PROJECTREF:ENCODED_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres
JWT_SECRET=ozingiz_uchun_uzun_va_tasodifiy_yozuv
